 # aoo bar1  
# comm1  
# comm2  
  boo bar5 baz "bar2" 'bar5.1' "bar 4" 'bar 5' bar\ 6; echo bar   
coo bar6 baz "bar2" 'bar3' "bar 4" 'bar 5' bar\ 6
#doo bar7
echo $a $b #barbar8
;;;;

function formula1 {
bar16 $(ls && echo bar17 )
ls -l | grep -f gender female&& echo bar && echo baz >/tmp/foo || some thing || someother thing && yetanother thing
a=$(ls -l | foo >/dev/null)
}

eoo $bc^$ef^foo bar11
foo $( ls -l | foo >/dev/null )
ls -l /facebook/friends | chart -f "relationship_status" >/tmp/redirout >>  /tmp/redirapp < blah
a && b
bar16 $(ls && echo bar17 )
ls -l | grep -f gender female&& echo bar && echo baz >/tmp/foo || some thing || someother thing && yetanother thing
a=$(ls -l | foo >/dev/null)
if ls -l file1; then echo file2; echo file3; else echo file4; fi

function formula2 # Top secret
# Secret formula
{
    a=$(ls -l | foo >/dev/null)
    if ls -l file1; then echo file2; echo file3; else echo file4; fi
}

if ! cond2;
    then
;;
   echo res2 
    echo res3; else

    echo res4
grep -f something | what < /dev/term >/dev/null 
    fi

for i in able baker charlie; do echo $i; done

for i in $foo $(ls) charlie

do

    echo something $i >/tmp/junk
    cat | blah | baz >/something
    ;;;

done
    
while E $a '<' 3; do echo still here; done

while a1 | b2 | c3

do

    echo still here
    cat | blah | baz >/something
    ;;;

done
